#include "std_lib_facilities.h"


void print(vector<double> v)
{
	cout<< '{';
	for (int i=0; i < v.size(); i++)
	{
		cout << v[i];
		if (i != v.size()-1)
		{
			cout << ",";
		}
		cout << '}';
	}
}

int main()
{
	vector<double> vd1(10);

	print (vd1);

	return 0;
}